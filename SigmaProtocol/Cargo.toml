[package]
name = "SigmaProtocol"
version = "0.1.0"
edition = "2024"

[dependencies]

num-bigint = { version = "0.4", features = ["serde"] } 
serde = { version = "1.0", features = ["derive"] }
num-traits = "0.2"

serde_json = "1.0"